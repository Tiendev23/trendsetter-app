import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

type Props = {};

export default function OrderDetailContent(props: Props) {
    return (
        <View>
            <Text>OrderDetailContent</Text>
        </View>
    );
}

const styles = StyleSheet.create({});